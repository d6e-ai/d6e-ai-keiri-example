# Base URL of the d6e instance.
#
# Managed deployments (e.g. https://your-d6e-instance.example.com)
# expose every d6e surface this app talks to on the same origin
# behind a reverse proxy
# that routes by path:
#   - /api/v1/...                  -> Rust API (file upload / delete /
#                                     workspaces / memberships)
#   - /api/workflows/...           -> SvelteKit frontend
#                                     (execute-by-intent)
#   - /api/chat-sessions[/...]     -> SvelteKit frontend
#   - /api/workspace-prompt-rules  -> SvelteKit frontend (init script)
#
# Because every endpoint lives on the same origin, this app collapses
# them behind a single base URL. If you ever need to split the Rust API
# and the SvelteKit frontend across different hosts, reintroduce a
# dedicated accessor in src/lib/server/env.ts and update the callers in
# src/lib/server/d6e-client.ts.
D6E_BASE_URL=https://your-d6e-instance.example.com

# UUID of the workspace this app should operate on.
# Every user logging into this app is checked for membership of this
# workspace; non-members are redirected to /auth/no-access.
#
# Where to find it: the workspace settings page
# ({D6E_BASE_URL}/{locale}/workspaces/{id}/settings) has a copy button
# in its Integration section (admin-only view), and the UUID also
# appears in every workspace URL.
D6E_WORKSPACE_ID=

# ----------------------------------------------------------------------
# OAuth2 (d6e-auth, brokered by the d6e instance) -- USER LOGIN
# ----------------------------------------------------------------------
#
# Each end user is redirected to D6E_AUTH_URL, authenticates with their
# own credentials, and is bounced back to D6E_AUTH_REDIRECT_URI with a
# one-shot authorization code. The server exchanges that code at the d6e
# INSTANCE's token endpoint (${D6E_BASE_URL}/api/v1/auth/token), which
# relays it to d6e-auth using the instance's OWN client credentials and
# returns a short-lived access token + long-lived refresh token already
# signed for the audience the instance accepts. Both land in HTTP-only
# cookies. This app therefore needs NO client secret of its own.
#
# Prerequisites (one-time setup):
#   1. Local dev callbacks need NO registration: loopback URLs
#      (localhost / 127.0.0.0/8 / [::1], any port) are always accepted.
#      Add this app's callback URL for every DEPLOYED environment to BOTH:
#        - the instance's registered_client.redirectUris on d6e-auth
#          (self-service for franchise owners/admins:
#          {D6E_AUTH_URL}/{locale}/account/franchise -> instance card
#          -> "Redirect URIs"), and
#        - the instance's ALLOWED_REDIRECT_URIS env var (comma-separated,
#          set by whoever operates the instance deployment),
#      e.g.
#        - https://your-deploy.vercel.app/auth/callback   (production)
#   2. Set D6E_AUTH_CLIENT_ID below to the instance's own client id.
#
# If you can change neither list (you are a third party with no
# franchise role and no say over the instance deployment), use the
# standalone-client variant documented in
# skills/d6e-auth-integration/SKILL.md instead.

# Base URL of the d6e-auth instance that hosts the login page.
D6E_AUTH_URL=https://www.d6e.ai

# OAuth client id used for login. In the instance-brokered flow set this
# to the d6e INSTANCE's OWN client id (the same value as the instance's
# D6E_AUTH_CLIENT_ID). No client secret is required here.
#
# Where to find it: the workspace settings page's Integration section
# shows "Client ID" with a copy button — visible to workspace ADMINS
# only. Ask a workspace admin if the section is missing for you.
D6E_AUTH_CLIENT_ID=

# Callback URL this app exposes for the OAuth2 redirect.
# Loopback URLs (localhost, any port) are always accepted. A deployed
# (non-loopback) URL must EXACTLY match an entry in BOTH the instance's
# registered_client.redirectUris and its ALLOWED_REDIRECT_URIS.
D6E_AUTH_REDIRECT_URI=http://localhost:5173/auth/callback

# ----------------------------------------------------------------------
# Optional: workspace prompt-rule bootstrap script
# ----------------------------------------------------------------------
#
# scripts/init-workspace.mjs registers this app's workspace prompt rule
# on d6e from your developer machine. It needs a refresh token that
# belongs to a workspace ADMIN (not an end user) so the POST to
# /api/workspace-prompt-rules is accepted.
#
# Copy the `auth-refresh` cookie value of a logged-in admin session on
# D6E_BASE_URL into the variable below. The script will exchange it for
# an access token automatically.
#
# This is independent from the end-user OAuth flow above and only used
# by `npm run init`.
D6E_INIT_REFRESH_TOKEN=
